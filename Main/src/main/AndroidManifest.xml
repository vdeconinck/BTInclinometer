<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="info.deconinck.inclinometer">

    <application
        android:name="info.deconinck.inclinometer.InitApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:theme"
        tools:ignore="AllowBackup">
        <activity
            android:name="info.deconinck.bt901.DataMonitorActivity"
            android:configChanges="orientation|screenSize|screenLayout"
            android:screenOrientation="fullSensor" />
        <activity
            android:name="info.deconinck.inclinometer.DeviceSelectionActivity"
            android:screenOrientation="fullSensor" />
        <activity android:name="info.deconinck.inclinometer.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>